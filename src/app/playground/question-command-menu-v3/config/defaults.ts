/**
 * Question Command Menu V3 - Default Configuration Values
 *
 * Re-exports from presets for backward compatibility.
 */

export { DEFAULT_CONFIG } from './presets'

// Re-export individual defaults for convenience
export {
  DEFAULT_CONFIG as DEFAULT_ANIMATION,
  DEFAULT_CONFIG as DEFAULT_LAYOUT,
  DEFAULT_CONFIG as DEFAULT_APPEARANCE,
  DEFAULT_CONFIG as DEFAULT_TRIGGER,
  DEFAULT_CONFIG as DEFAULT_CONTENT,
  DEFAULT_CONFIG as DEFAULT_CONTENT_CONFIGS,
} from './presets'

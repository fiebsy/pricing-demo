# StackingNav Motion Playground

## Purpose

Experimental StackingNav variant using Motion's `layout="position"` for anchored item repositioning.

## URL

`/playground/stacking-nav-motion`

## Files

| File | Purpose |
|------|---------|
| `page.tsx` | Playground page |
| `config/types.ts` | Config interface (no clip-path fields) |
| `config/options.ts` | Control option arrays |
| `config/nav-items.ts` | Demo data |
| `config/presets.ts` | Default presets |
| `panels/panel-config.ts` | UnifiedControlPanel config |

## Component Source

`src/components/ui/features/experimental/stacking-nav-motion/`

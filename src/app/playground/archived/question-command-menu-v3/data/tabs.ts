/**
 * Question Command Menu V3 - Default Tab Options
 *
 * Fallback data for the tabs content type.
 */

import type { FilterOption } from '../config/types'

export const DEFAULT_TAB_OPTIONS: FilterOption[] = [
  { id: 'recent', label: 'Recent' },
  { id: 'starred', label: 'Starred' },
  { id: 'all', label: 'All' },
]
